{
    "private": true,
    "scripts": {
        "test": "yarn build && anchor test",
        "build": "yarn tsc --build && anchor build",
        "prepublish": "yarn build",
        "publish": "yarn workspace @identity.com/cryptid-idl publish"
    },
    "workspaces": [
        "packages/client/idl",
        "packages/client/middleware/checkPass",
        "packages/client/core",
        "packages/client/cryptid",
        "packages/client/cli",
        "packages/tests"
    ],
    "packageManager": "yarn@3.2.3",
    "devDependencies": {
        "mocha": "^10.0.0",
        "ts-mocha": "^10.0.0",
        "typescript": "^4.8.3"
    }
}
