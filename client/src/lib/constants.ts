import {PublicKey} from "@solana/web3.js";
import {ExtendedCluster} from "../types/solana";

export const DOA_PROGRAM_ID = new PublicKey('TODO'); // TODO @brett
export const DEFAULT_CLUSTER: ExtendedCluster = 'mainnet-beta';
