{
  "name": "@identity.com/cryptid",
  "version": "0.3.0-alpha.8",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "yarn clean",
    "lint": "eslint src/**/*.ts",
    "build": "tsc --build"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@identity.com/cryptid-core": "0.3.0-alpha.8",
    "@identity.com/cryptid-middleware-check-pass": "0.3.0-alpha.8",
    "@identity.com/cryptid-middleware-time-delay": "0.3.0-alpha.8",
    "@identity.com/cryptid-middleware-check-recipient": "0.3.0-alpha.8"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
